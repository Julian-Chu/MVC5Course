@model int[]

@for (int i = 0; i < Model.Count(); i++)
{
    @Model[i]
}
<br/>
@for (int i = 0; i < Model.Count(); i++)
{

    @(Model[i]) @(" ")
}
<br/>
@for (int i = 0; i < Model.Count(); i++)
{
    @(Model[i]) @(" ")
}
</br>
@for (int i = 0; i < Model.Count(); i++)
{

    <text>@(Model[i])</text>
}




<ul>
@for (int i = 0; i < Model.Count(); i++)
{

    <li>@Model[i]</li>
}

</ul>
 
